#include <stdio.h>
#include <stdlib.h>
/**
 * main - function
 * description - in the alphabet
 * Return: 0 (successs);
 */
int main(void)
{
	char mi = 'a';

	for (mi = 'a'; mi <= 'z'; mi++)
	putchar(mi);
	putchar('\n');
	return (0);
}

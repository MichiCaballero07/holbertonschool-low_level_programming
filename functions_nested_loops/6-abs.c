# include "main.h"
/**
 * _abs - function that computes the absolute value
 * @number:integer input
 * Return: absolute value of number
 */
int _abs(int number)
{
	if (number > 0)
	return (-1 * number);
	else
	{
		return (1 * number);
	}
}
